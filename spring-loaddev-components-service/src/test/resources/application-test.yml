spring:
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
  threads:
    virtual:
      enabled: true
  r2dbc:
    url: "r2dbc:tc:postgresql:///databasename?TC_IMAGE_TAG=18.0"
    name: "sa"
  sql:
    init:
      mode: always  

eureka:
  client:
    enabled: false

management:
  otlp:
    metrics:
      export:
        enabled: false
  tracing:
    enabled: false

otel:
  sdk:
    disabled: true