package ca.zhoozhoo.loaddev.security;

import static org.junit.jupiter.api.Assertions.assertTrue;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

/**
 * Integration test placeholder for {@link CurrentUser} annotation.
 * <p>
 * Full integration testing of the {@code @CurrentUser} parameter injection is performed
 * in the consuming service modules (loads-service, rifles-service) where the complete
 * Spring Security context and WebFlux infrastructure are available.
 *
 * @author Zhubin Salehi
 */
class CurrentUserIntegrationTest {
    @Test
    @DisplayName("Placeholder assertion")
    void placeholder() { assertTrue(true); }
}
