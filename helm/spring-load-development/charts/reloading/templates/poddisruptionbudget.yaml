{{/*
PodDisruptionBudgets for all microservices
*/}}

{{/* API Gateway PodDisruptionBudget */}}
{{- if .Values.apiGateway.enabled }}
{{- include "reloading.microservice.podDisruptionBudget" (dict "componentName" "api-gateway" "config" .Values.apiGateway "context" .) }}
{{- end }}

{{/* Loads Service PodDisruptionBudget */}}
{{- if .Values.loadsService.enabled }}
{{- include "reloading.microservice.podDisruptionBudget" (dict "componentName" "loads-service" "config" .Values.loadsService "context" .) }}
{{- end }}

{{/* Components Service PodDisruptionBudget */}}
{{- if .Values.componentsService.enabled }}
{{- include "reloading.microservice.podDisruptionBudget" (dict "componentName" "components-service" "config" .Values.componentsService "context" .) }}
{{- end }}

{{/* Rifles Service PodDisruptionBudget */}}
{{- if .Values.riflesService.enabled }}
{{- include "reloading.microservice.podDisruptionBudget" (dict "componentName" "rifles-service" "config" .Values.riflesService "context" .) }}
{{- end }}

{{/* MCP Server PodDisruptionBudget */}}
{{- if .Values.mcpServer.enabled }}
{{- include "reloading.microservice.podDisruptionBudget" (dict "componentName" "mcp-server" "config" .Values.mcpServer "context" .) }}
{{- end }}
