spring:
  application:
    name: discovery-server
  config:
    import: optional:configserver:${SPRING_CONFIG_IMPORT:http://localhost:8888/}
management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      probes:
        enabled: true