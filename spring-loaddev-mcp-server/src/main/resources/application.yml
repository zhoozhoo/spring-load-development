spring:
  application:
    name: mcp-server
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888/}
  profiles: 
    active: @spring.profiles.active@
---
spring:
  config:
    activate:
      on-profile: native
  cloud:
    refresh:
      enabled: false